# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : pandoc
version    : '3.10'
release    : 17
source     :
    - https://github.com/jgm/pandoc/archive/refs/tags/3.10.tar.gz : fc82815542c29802d087c25e2c1421146030806b71255ec40fc0e828fe1df877
homepage   : https://pandoc.org
license    : GPL-2.0-or-later
component  : office
summary    : Pandoc is a Haskell library for converting from one markup format to another, and a command-line tool that uses this library
description: |
    Pandoc is a Haskell library for converting from one markup format to another, and a command-line tool that uses this library
builddeps  :
    - ghc
    - haskell-cabal-install
networking : true
setup      : |
    cabal update
    cabal configure -O2 %JOBS% \
      --disable-executable-dynamic \
      --ghc-options="-H512m %JOBS%" \
      -f embed_data_files
build      : |
    cabal build pandoc-cli
install    : |
    mkdir -p $installdir/usr/bin
    cabal install --installdir=$installdir/usr/bin --install-method=copy pandoc-cli 
    %install_license COPYING.md
check      : |
    cabal test
